import{useState}from"react";import{Link,useLocation,useNavigate}from"react-router-dom";import{authApi}from"../../lib/api/auth";import{ApiError}from"../../lib/api/errors";import{productConfig}from"../../lib/config/product";import{useSession}from"../../lib/auth/session";import"./auth-form.css";
type Mode="login"|"register"|"confirm"|"forgot";const copy={login:["Welcome back","Continue your applications with clarity."],register:["Create your EliteApply account","Build one calm home for every application."],confirm:["Confirm your email","Enter the code sent to your inbox."],forgot:["Reset your password","We’ll send a one-time code to your email."]}as const;
export function AuthPage({mode}:{mode:Mode}){const nav=useNavigate(),location=useLocation(),setTokens=useSession(s=>s.setTokens),[busy,setBusy]=useState(false),[error,setError]=useState(""),[success,setSuccess]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const data=Object.fromEntries(new FormData(e.currentTarget));try{if(mode==="login"){const result=await authApi.login({email:String(data.email),password:String(data.password)});setTokens(result);const returnTo=new URLSearchParams(location.search).get("returnTo");nav(returnTo&&returnTo.startsWith("/app")?returnTo:"/app/dashboard",{replace:true})}if(mode==="register"){await authApi.register({email:String(data.email),password:String(data.password),full_name:String(data.full_name)||null,accepted_terms_version:productConfig.legal.currentTermsVersion,marketing_opt_in:data.marketing_opt_in==="on"});nav(`/confirm-email?email=${encodeURIComponent(String(data.email))}`)}if(mode==="confirm"){await authApi.confirm({email:String(data.email),code:String(data.code)});setSuccess("Email confirmed. You can now sign in.")}if(mode==="forgot"){await authApi.forgot({email:String(data.email)});nav(`/reset-password?email=${encodeURIComponent(String(data.email))}`)}}catch(x){setError(x instanceof ApiError?x.message:"Something went wrong.")}finally{setBusy(false)}}const[title,subtitle]=copy[mode];return <main className="auth-layout"><section className="auth-brand"><Link to="/" className="brand">EliteApply</Link><div><h2>Your applications.<br/>One clear system.</h2><p>Organise every programme, deadline, document and decision without losing the story you want to tell.</p></div></section><section className="auth-panel"><form className="auth-form" onSubmit={submit} aria-describedby={error?"form-error":undefined}><div><h1>{title}</h1><p>{subtitle}</p></div>{(mode==="register")&&<label>Full name<input name="full_name" autoComplete="name"/></label>}<label>Email address<input name="email" type="email" required autoComplete="email" defaultValue={new URLSearchParams(location.search).get("email")??""}/></label>{mode==="confirm"&&<label>Confirmation code<input name="code" required minLength={4} maxLength={10} autoComplete="one-time-code"/></label>}{(mode==="login"||mode==="register")&&<label>Password<input name="password" type="password" required minLength={mode==="login"?1:8} maxLength={128} autoComplete={mode==="login"?"current-password":"new-password"}/>{mode!=="login"&&<small>At least 8 characters</small>}</label>}{mode==="register"&&<><label className="check"><input type="checkbox" required/>I agree to the <Link to="/terms">Terms</Link> and <Link to="/privacy">Privacy Policy</Link>.</label><label className="check"><input name="marketing_opt_in" type="checkbox"/>Send useful product updates.</label></>}{error&&<p className="form-error" id="form-error" role="alert">{error}</p>}{success&&<p className="form-success" role="status">{success}</p>}<button className="primary" disabled={busy}>{busy?"Please wait…":title}</button><div className="auth-links">{mode==="login"&&<><Link to="/forgot-password">Forgot password?</Link><span>New here? <Link to="/register">Create account</Link></span></>}{mode!=="login"&&<Link to="/login">Back to sign in</Link>}</div></form></section></main>}
