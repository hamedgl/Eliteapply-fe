import{useQuery}from"@tanstack/react-query";import{ArrowRight,CalendarDays,CheckSquare,FileWarning,FolderKanban,GraduationCap}from"lucide-react";import{platformApi,safeDashboard}from"../../lib/api/platform";import{queryKeys}from"../../lib/api/queryKeys";import{useSession}from"../../lib/auth/session";export function DashboardPage(){const user=useSession(s=>s.user),query=useQuery({queryKey:queryKeys.dashboard,queryFn:async()=>safeDashboard(await platformApi.dashboard())});if(query.isPending)return <div className="page"><p role="status">Loading your dashboard…</p></div>;if(query.isError)return <div className="page error-state"><h1>We couldn’t load your dashboard</h1><p>Your data is safe. Check your connection and try again.</p><button className="primary" onClick={()=>query.refetch()}>Try again</button></div>;const d=query.data;return <div className="page dashboard"><header><div><h1>Good morning{user?.full_name?`, ${user.full_name.split(" ")[0]}`:""}</h1><p>Your application command centre</p></div></header><section className="profile-progress"><div><GraduationCap/><div><h2>Complete your academic profile</h2><p>A strong academic profile helps institutions understand your background.</p><a href="/app/unavailable">Continue profile <ArrowRight/></a></div></div><div className="progress" aria-label={`${d.profile_completion_percent}% profile complete`}><strong>{d.profile_completion_percent}%</strong><span>Complete</span></div></section><div className="dashboard-grid"><Panel icon={FolderKanban} title="Applications by stage">{Object.keys(d.applications_by_stage).length?<ul>{Object.entries(d.applications_by_stage).map(([stage,count])=><li key={stage}><span>{stage.replaceAll("_"," ")}</span><strong>{count}</strong></li>)}</ul>:<Empty text="No applications yet" detail="Your application stages will appear here."/>}</Panel><Panel icon={CalendarDays} title="Upcoming deadlines"><Empty text={d.upcoming_deadlines.length?`${d.upcoming_deadlines.length} upcoming deadlines`:"No upcoming deadlines"} detail="Start an application to see deadlines here."/></Panel><Panel icon={FileWarning} title="Missing documents"><Metric value={d.missing_documents} label="documents need attention"/></Panel><Panel icon={CheckSquare} title="Open tasks"><Metric value={d.open_tasks} label="tasks across your applications"/></Panel><section className="panel recommendation"><h2>Recommended next step</h2><p>{d.recommended_next_action}</p><a href="/app/unavailable" className="primary">Continue <ArrowRight/></a></section></div></div>}function Panel({icon:Icon,title,children}:{icon:typeof FolderKanban;title:string;children:React.ReactNode}){return <section className="panel"><h2><Icon/>{title}</h2>{children}</section>}function Empty({text,detail}:{text:string;detail:string}){return <div className="empty"><strong>{text}</strong><span>{detail}</span></div>}function Metric({value,label}:{value:number;label:string}){return <div className="metric"><strong>{value}</strong><span>{label}</span></div>}
