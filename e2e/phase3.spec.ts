import{expect,test}from"@playwright/test";const user={id:"00000000-0000-4000-8000-000000000001",identity_subject:"test",email:"maya@example.test",full_name:"Maya Chen",headline:null,phone_number:null,avatar_url:null,is_email_verified:true,is_active:true,is_admin:false,marketing_opt_in:false,created_at:"2026-01-01T00:00:00Z",updated_at:"2026-01-01T00:00:00Z",last_login_at:null};const doc={id:"00000000-0000-4000-8000-000000000010",application_id:null,document_type:"statement_of_purpose",cv_mode:null,title:"MSc Computer Science — Statement of Purpose",prompt_text:null,target_requirements:{},content:{text:"My academic journey began with a curiosity about complex systems."},evidence_map:{},word_limit:1000,character_limit:null,template_id:null,theme:{},status:"draft",version:1,created_at:"2026-01-01T00:00:00Z",updated_at:"2026-01-02T00:00:00Z"};test.beforeEach(async({page})=>{await page.route("**/api/v1/**",async route=>{const url=route.request().url(),method=route.request().method();if(url.endsWith("/auth/refresh"))return route.fulfill({json:{access_token:"test",id_token:"test",expires_in:3600}});if(url.endsWith("/users/me"))return route.fulfill({json:user});if(url.endsWith("/platform/capabilities"))return route.fulfill({json:[]});if(url.endsWith(`/writing-studio/documents/${doc.id}/revisions`))return route.fulfill({json:[]});if(url.endsWith(`/writing-studio/documents/${doc.id}`)&&method==="GET")return route.fulfill({json:doc});if(url.endsWith(`/writing-studio/documents/${doc.id}`)&&method==="PATCH")return route.fulfill({json:{...doc,version:2,content:{text:"Updated statement"}}});if(url.endsWith("/writing-studio/documents"))return route.fulfill({json:[doc]});return route.fulfill({json:{}})});});test("writing library and editor are responsive and save-state aware",async({page})=>{await page.goto("/app/writing");await expect(page.getByRole("heading",{name:"Writing Studio"})).toBeVisible();await page.getByText(doc.title).click();await expect(page.getByLabel("Document content")).toContainText("academic journey");await page.getByLabel("Document content").fill("Updated statement");await expect(page.getByText("Unsaved")).toBeVisible();await page.screenshot({path:"/tmp/eliteapply-phase3-desktop.png",fullPage:true});await page.setViewportSize({width:390,height:844});await page.reload();await expect(page.getByLabel("Document content")).toBeVisible();await page.screenshot({path:"/tmp/eliteapply-phase3-mobile.png",fullPage:true})});test("public referee code stays out of the URL",async({page})=>{let header="";await page.route("**/api/v1/referee/**",async route=>{header=route.request().headers()["x-reference-code"]??"";return route.fulfill({json:{referee_role:"professor",institution:"Example University"}})});await page.goto("/referee/academic-reference/token-only");await page.getByLabel("Reference code").fill("separate-code");await page.getByRole("button",{name:"Continue securely"}).click();await expect.poll(()=>header).toBe("separate-code");expect(page.url()).not.toContain("separate-code")});
